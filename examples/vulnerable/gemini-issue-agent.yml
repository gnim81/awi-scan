name: vulnerable gemini issue agent
on:
  issues:
    types: [opened]
permissions:
  issues: write
jobs:
  agent:
    runs-on: ubuntu-latest
    steps:
      - name: Ask Gemini to triage the issue
        run: |
          gemini --prompt "${{ github.event.issue.body }}"

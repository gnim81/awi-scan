name: vulnerable aider pull request agent
on:
  pull_request_target:
permissions:
  contents: write
  pull-requests: write
jobs:
  agent:
    runs-on: ubuntu-latest
    steps:
      - name: Ask Aider to review the pull request
        run: |
          aider --message "${{ github.event.pull_request.body }}"
